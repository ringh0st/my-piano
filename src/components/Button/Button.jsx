import React from 'react';
import {ButtonContainer} from './Button.style';

const Button = ({text}) =>{

    return(
        <ButtonContainer>
            {text}
        </ButtonContainer>
    )
}
export default Button